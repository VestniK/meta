package test;

int dist(int x, int y)
{
    return x*x + y*y;
}

int constFoo()
{
    return 5;
}

int poly(int x)
{
    int common = x+3;
    return 5*common*common - 6*common + 3;
}

int distCall(int x)
{
    return dist(x, x);
}

int varsUsage(int x)
{
    int y;
    int z = 5;
    y = x + z;
    z = y*x;
    return x + y + z;
}

int assigment(int x)
{
    int a;
    int b;
    int c = a = b = x + 5;
    return a + b + c;
}
